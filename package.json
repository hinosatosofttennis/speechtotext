{
  "name": "speech-to-text-proxy",
  "version": "1.0.0",
  "description": "Speech-to-Text API Proxy for GitHub.io",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@google-cloud/speech": "^6.0.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}

